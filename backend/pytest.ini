[pytest]
# pytest-asyncio warns on every run that this is unset and that its default will
# change. Naming it explicitly silences the warning and pins current behaviour,
# so a future pytest-asyncio cannot quietly move fixtures to a different loop
# scope underneath the suite.
asyncio_default_fixture_loop_scope = function
